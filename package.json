{
  "name": "mtg-engine",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "dev:api": "pnpm --filter @mtg-engine/api dev",
    "dev:web": "pnpm --filter @mtg-engine/web dev",
    "format": "oxfmt package.json pnpm-workspace.yaml tsconfig.base.json vitest.config.ts apps packages",
    "format:check": "oxfmt --check package.json pnpm-workspace.yaml tsconfig.base.json vitest.config.ts apps packages",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "oxfmt": "^0.46.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
